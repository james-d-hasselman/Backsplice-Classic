using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Backsplice;

namespace UnitTests
{
    [TestClass]
    public class TestCreatePaperwork
    {
        [TestMethod]
        public void TestMethod1()
        {
            // arrange for create paperwork

            // create some paperwork

            // assert the following:
                // the correct number of files were created
                // the correct no template action was taken
                // the files were placed in the correct location
                // the scouts were sorted properly
                // each course has the correct number of scouts
        }
    }
}
